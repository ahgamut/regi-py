#include <regi.h>

namespace regi
{
    void GameState::setup() {
        // in a server-style setup we'd initialize the connections here
    }

    void GameState::selectDefense(Player &player, int damage) {}

    void GameState::selectAttack(Player &player) {}

} /* namespace regi */
